INITIAL

Backend:
Convert chosen pixel into java code
Group by colors
Each pixel = drawRect

Frontend:
JFrame based
Color in pixel by pixel
Save output = html and .java file which can display the created image as an applet


ADDITIONAL
Work on UI
Define classes and inheritance
Define background color
Output in bigger rectangles
Save & Parse for later use (in Java & as an image)

verification of valid class name
private variables (security upgrades)